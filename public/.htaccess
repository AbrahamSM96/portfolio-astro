RewriteEngine On

# Redirect www to non-www
RewriteCond %{HTTP_HOST} ^www\.abrahamsm\.com [NC]
RewriteRule ^(.*)$ https://abrahamsm.com/$1 [R=301,L]

# Force HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}/$1 [R=301,L]
